{
"project*context": "Đang xây dựng hệ thống AI E-commerce Chatbot cho cửa hàng giày dép trên Facebook Messenger bằng n8n + PostgreSQL + Gemini API. Hệ thống mục tiêu là multimodal (TEXT, IMAGE, AUDIO), có debounce/gom nhiều tin nhắn, Customer Intelligence, Multi-Agent AI (Supervisor -> Sales/CRM/Order), Product/Variant/Inventory Tools, RAG, Vision, Human-in-the-Loop qua Telegram, Payment/VietQR, Output Validator, Response Dispatcher và Monitoring/AI Logs. Kiến trúc được thống nhất theo hướng nhiều workflow/module thay vì một workflow khổng lồ. Hiện tại đang hoàn thiện WF01 — Messenger Ingestion & Debounce; WF02 — Message Processing là bước tiếp theo. Source of Truth của dự án là MASTER_PROJECT.md, trong đó quy định Messenger là input chính qua Webhook, session_id = messenger_id + page_id, Customer Profile khác Chat Memory, Handoff Status Check phải trước AI, Handoff Detector dùng để chuyển sang người, RAG là Tool của Agent, Inventory là source of truth cho tồn kho, Output Validator nằm trước Response, và AI execution phải được log.",
"current_nodes_state": [
{
"node_id_or_name": "WF01 - Messenger Ingestion & Debounce",
"purpose": "Workflow nhận event Messenger, chuẩn hóa message, phân loại tối thiểu TEXT/IMAGE/AUDIO, lưu message vào PostgreSQL, debounce/gom các message liên tiếp và tạo một payload thống nhất để gọi WF02.",
"modifications_made": "WF01 được tách khỏi WF02. Đã loại bỏ Rate Limit khỏi luồng hiện tại vì làm mất các field như session_id và làm phức tạp flow trong khi Debounce là tối ưu quan trọng hơn. Đã quyết định chỉ triển khai 3 loại TEXT, IMAGE, AUDIO ở phiên bản hiện tại. Đã thống nhất không gọi AI Supervisor trong tầng ingestion. Đã thống nhất lưu message trước khi các bước AI xử lý sâu. Đã test thành công việc gom TEXT + IMAGE và TEXT/IMAGE/AUDIO thành một output duy nhất.",
"current_code_or_config": "WF01 flow hiện tại: Webhook -> Data Processor -> MESSAGE ROUTER -> TEXT Processor / IMAGE Processor / AUDIO Processor -> Save Message -> Message Debounce -> Get & Claim Messages -> Combine Messages. Webhook phải trả 200 OK nhanh. MESSAGE ROUTER hiện tập trung 3 output TEXT, IMAGE, AUDIO. Không dùng Rate Limit ở phiên bản hiện tại. Debounce Window hiện dùng 3 giây."
},
{
"node_id_or_name": "Webhook",
"purpose": "Nhận event từ Facebook Messenger.",
"modifications_made": "Giữ Webhook là entry point chính của Messenger. Đã thống nhất không để Webhook chờ Gemini/AI/Telegram. Messenger phải nhận HTTP 200 sớm.",
"current_code_or_config": "HTTP Method: POST. Webhook là entry point của WF01. Response nên dùng Respond to Webhook/200 OK ngay. Production URL của Messenger đang hoạt động theo workflow hiện tại."
},
{
"node_id_or_name": "Respond to Webhook",
"purpose": "Trả xác nhận HTTP 200 cho Messenger thật nhanh, không chờ AI.",
"modifications_made": "Được xác định là một phần của ingestion để tránh Messenger phải chờ các bước xử lý phía sau.",
"current_code_or_config": "Response Code: 200. Response body có thể là {\"status\":\"ok\"}. Không chứa logic AI."
},
{
"node_id_or_name": "Data Processor",
"purpose": "Chuẩn hóa payload Messenger thành dữ liệu dùng chung cho tất cả node phía sau.",
"modifications_made": "Đã thống nhất output có messenger_id, page_id, session_id, timestamp, message_id, message_type, text, attachment_types, attachment_urls, attachments. session_id phải được tạo từ messenger_id + page_id. Các trường media phải được giữ nguyên, không biến IMAGE/AUDIO thành TEXT giả.",
"current_code_or_config": "Output contract thực tế đã xác nhận: {\"messenger_id\":\"27855554264111350\",\"page_id\":\"1247306088468500\",\"session_id\":\"27855554264111350_1247306088468500\",\"timestamp\":1786870390124,\"message_id\":\"...\",\"message_type\":\"TEXT|IMAGE|AUDIO\",\"text\":\"... hoặc null\",\"attachment_types\":[],\"attachment_urls\":[],\"attachments\":[]}. Với IMAGE/AUDIO, text có thể null nhưng attachments và attachment_urls phải chứa URL thực."
},
{
"node_id_or_name": "MESSAGE ROUTER",
"purpose": "Phân loại event thành TEXT, IMAGE, AUDIO để đi vào các Processor tương ứng.",
"modifications_made": "Đã rút gọn scope hiện tại từ nhiều loại TEXT/IMAGE/VIDEO/AUDIO/FILE/OTHER xuống chỉ TEXT, IMAGE, AUDIO theo yêu cầu hiện tại. Đã sửa lỗi rule AUDIO: leftValue phải là expression thực sự ={{ $json.message_type}}, không phải chuỗi '{{ $json.message_type }}'. Đã xác nhận AUDIO hiện đã đi qua Router thành công.",
"current_code_or_config": "Switch node, Rules mode. Output hiện tại: TEXT, IMAGE, AUDIO. Rule dạng string equals. TEXT: leftValue ={{ $json.message_type }}, rightValue TEXT. IMAGE: leftValue ={{ $json.message_type }}, rightValue IMAGE. AUDIO: leftValue ={{ $json.message_type }}, rightValue AUDIO. Không dùng Switch để loại trừ media sau này trong WF02 vì một batch có thể đồng thời có text + image + audio."
},
{
"node_id_or_name": "TEXT Processor",
"purpose": "Chuẩn hóa text message thành message_content thực tế để lưu DB và debounce.",
"modifications_made": "Đã sửa để không tạo literal '{{ $json.message_content }}'. Đã xác định lỗi cũ là dữ liệu upstream chứa expression như text thay vì giá trị thật. Text Processor hiện lấy trực tiếp data.text.",
"current_code_or_config": "Code hiện tại khuyến nghị: const data = $json; return [{ json: { ...data, message_content: String(data.text ?? '').trim(), media: [], processing_status: 'READY' } }];. Với tin 'chào' output phải có message_content='chào', không phải '{{ $json.message_content }}'."
    },
    {
      "node_id_or_name": "IMAGE Processor",
      "purpose": "Chuẩn hóa IMAGE message, giữ URL ảnh và trạng thái xử lý; chưa gọi Vision tại tầng Processor hiện tại.",
      "modifications_made": "Đã tách IMAGE khỏi TEXT. Đã thống nhất không dùng message.text làm dữ liệu bắt buộc cho IMAGE. IMAGE phải lưu media URL và có thể dùng message_content='[IMAGE]' hoặc chuỗi rỗng; dữ liệu thật nằm trong media/attachments.",
      "current_code_or_config": "Code hiện tại theo hướng chuẩn hóa: const data = $json; const attachments = Array.isArray(data.attachments) ? data.attachments : []; const media = attachments.filter(a => a?.type === 'image' && a?.payload?.url).map(a => ({type:'IMAGE', url:a.payload.url})); return [{json:{...data, message_content:'[IMAGE]', media, processing_status:'READY'}}];"
    },
    {
      "node_id_or_name": "AUDIO Processor",
      "purpose": "Chuẩn hóa AUDIO message, giữ URL audio để xử lý Speech-to-Text sau này.",
      "modifications_made": "Đã sửa flow để AUDIO không bị lỗi do Message Debounce yêu cầu Message nhưng AUDIO không có text. AUDIO được giữ bằng media URL thay vì cố lấy message.text. Đã xác nhận AUDIO từ Messenger được lưu DB và Combine Messages xuất được media type AUDIO + URL .ogg.",
      "current_code_or_config": "Code hiện tại theo hướng chuẩn hóa: const data = $json; const attachments = Array.isArray(data.attachments) ? data.attachments : []; const media = attachments.filter(a => a?.type === 'audio' && a?.payload?.url).map(a => ({type:'AUDIO', url:a.payload.url})); return [{json:{...data, message_content:'[AUDIO]', media, processing_status:'READY'}}];"
    },
    {
      "node_id_or_name": "Save Message",
      "purpose": "Lưu toàn bộ message đã chuẩn hóa vào PostgreSQL, đồng thời bảo toàn session_id và metadata multimodal.",
      "modifications_made": "Đã sửa lỗi null session_id bằng cách luôn truyền session_id từ $json.session_id. Đã sửa lỗi lưu literal '{{ $json.message_content }}' thành giá trị thực. Đã xác nhận PostgreSQL đang lưu đúng IMAGE và AUDIO trong cột messages của bảng conversations. Không thay đổi schema chỉ vì IMAGE/AUDIO.",
      "current_code_or_config": "SQL hiện tại: INSERT INTO conversations ( session_id, message, created_at ) VALUES ( $1, $2::jsonb, NOW() ) RETURNING id, session_id, message, created_at; Query Parameters được truyền dạng array [ $json.session_id, JSON.stringify({ message_id:$json.message_id, messenger_id:$json.messenger_id, page_id:$json.page_id, timestamp:$json.timestamp, message_type:$json.message_type, text:$json.message_content || $json.text || null, attachments:$json.attachments || [], attachment_types:$json.attachment_types || [], attachment_urls:$json.attachment_urls || [], media:$json.media || null, processing_status:$json.processing_status || 'NONE' }) ]."
},
{
"node_id_or_name": "conversations.message column",
"purpose": "Lưu payload JSON của từng message trong bảng conversations.",
"modifications_made": "Đã kiểm tra thực tế database. IMAGE và AUDIO đều đang được lưu đúng schema JSON với text, page_id, timestamp, message_id, attachments, message_type, messenger_id, attachment_urls, attachment_types.",
"current_code_or_config": "Ví dụ IMAGE thực tế: {\"text\":null,\"page_id\":\"1247306088468500\",\"timestamp\":1786908109283,\"message_id\":\"...\",\"attachments\":[{\"type\":\"image\",\"payload\":{\"url\":\"https://...\"}}],\"message_type\":\"IMAGE\",\"messenger_id\":\"27855554264111350\",\"attachment_urls\":[{\"url\":\"https://...\",\"type\":\"image\"}],\"attachment_types\":[\"image\"]}. Ví dụ AUDIO thực tế: {\"text\":null,\"page_id\":\"1247306088468500\",\"timestamp\":1786908214606,\"message_id\":\"...\",\"attachments\":[{\"type\":\"audio\",\"payload\":{\"url\":\"https://...ogg\"}}],\"message_type\":\"AUDIO\",\"messenger_id\":\"27855554264111350\",\"attachment_urls\":[{\"url\":\"https://...ogg\",\"type\":\"audio\"}],\"attachment_types\":[\"audio\"]}."
},
{
"node_id_or_name": "Message Debounce",
"purpose": "Gom các tin nhắn liên tiếp của cùng session trước khi chuyển sang WF02, tránh gọi AI nhiều lần cho nhiều message ngắn liên tiếp.",
"modifications_made": "Đã sửa lỗi Message is required. Nguyên nhân cũ là dùng {{$json.message.text || ''}} cho IMAGE/AUDIO hoặc {{$json.message_content}} ở root sau Save Message. Vì Save Message output có root message object, Debounce phải lấy toàn bộ message object.",
"current_code_or_config": "Credential: Redis account. Session ID: ={{ $json.session_id }}. Message: ={{ JSON.stringify($json.message) }}. Debounce Window: 3 seconds. First Message Behavior: None. Debounce phải có Message ngay cả khi text null, nên không dùng chỉ message.text."
},
{
"node_id_or_name": "Get & Claim Messages",
"purpose": "Lấy các message đã debounce, claim để tránh xử lý trùng và chuẩn bị cho Combine Messages.",
"modifications_made": "Đã giữ node trong WF01 sau Message Debounce. Logic hướng tới việc lấy các message chưa xử lý theo session và tránh race condition. Đây là phần queue/claim, không gọi AI.",
"current_code_or_config": "Node PostgreSQL Execute Query. Logic claim được thống nhất theo hướng FOR UPDATE SKIP LOCKED/claim state. Cấu trúc record thực tế có id, session_id, message, created_at, claimed_at, processed_at. Một số record thực tế ví dụ id 219/220/221/222/225/226 đã được claim và combine thành batch."
},
{
"node_id_or_name": "Combine Messages",
"purpose": "Gom một batch message đã claim thành một payload thống nhất gồm combined_text, media, message_ids, session_id, messenger_id, page_id.",
"modifications_made": "Đã sửa Combine để xử lý TEXT + IMAGE + AUDIO cùng lúc. Đã sửa cấu trúc media từ dạng url object lồng nhau thành {type:'IMAGE'|'AUDIO', url:'https://...'}. Đã xác nhận output thực tế hoạt động: combined_text='tôi là hoàng' và media=[{type:'IMAGE',url:'...'}]; AUDIO cũng đã cho combined_text='' và media=[{type:'AUDIO',url:'...ogg'}].",
"current_code_or_config": "Code hiện tại: const items=$input.all(); if(!items.length) return []; const first=items[0].json; const messageIds=[]; const textParts=[]; const media=[]; for(const item of items){const row=item.json; const message=row.message||{}; if(row.id!=null) messageIds.push(String(row.id)); if(typeof message.text==='string' && message.text.trim() && !message.text.includes('{{ $json.')) textParts.push(message.text.trim()); const attachmentUrls=Array.isArray(message.attachment_urls)?message.attachment_urls:[]; for(const attachment of attachmentUrls){ if(!attachment?.url) continue; const type=String(attachment.type||'').toUpperCase(); if(type==='IMAGE') media.push({type:'IMAGE',url:attachment.url}); if(type==='AUDIO') media.push({type:'AUDIO',url:attachment.url}); }} return [{json:{session_id:first.session_id,messenger_id:first.message?.messenger_id||first.messenger_id,page_id:first.message?.page_id||first.page_id,message_ids:messageIds,combined_text:textParts.join(' '),media,message_count:items.length,created_at:first.created_at}}];"
    },
    {
      "node_id_or_name": "Execute Workflow -> WF02",
      "purpose": "Gọi WF02 sau khi WF01 đã debounce và combine một batch message.",
      "modifications_made": "Đã thống nhất đây là điểm kết thúc của WF01. WF02 sẽ nhận một item thay vì từng Messenger event riêng lẻ.",
      "current_code_or_config": "Input dự kiến: {session_id,messenger_id,page_id,message_ids,combined_text,media,message_count,created_at}. Ví dụ thực tế đã test: session_id='27855554264111350_1247306088468500', messenger_id='27855554264111350', message_ids=['221','222'], combined_text='tôi là hoàng', media=[{type:'IMAGE',url:'https://...'}], message_count=2."
    },
    {
      "node_id_or_name": "Rate Limit",
      "purpose": "Đã từng được đề xuất để giới hạn spam theo messenger_id nhưng đã quyết định bỏ ở phiên bản hiện tại.",
      "modifications_made": "Đã tạo thử bảng message_rate_limit và IF allowed, nhưng bỏ hoàn toàn khỏi WF01 vì làm mất session_id trong output intermediate và vì Debounce mới là tối ưu quan trọng cho workload thực tế. Rate limiting có thể bổ sung sau khi hệ thống chạy ổn.",
      "current_code_or_config": "Không còn nằm trong flow hiện tại. Không sử dụng message_rate_limit trong phiên bản hiện tại."
    },
    {
      "node_id_or_name": "WF02 — Message Processing",
      "purpose": "Workflow tiếp theo cần triển khai. Nhận payload đã combine từ WF01, kiểm tra Human Handoff trước AI, chạy Input Guardrail, Quick Reply/Fast Track, sau đó xử lý TEXT/IMAGE/AUDIO theo hướng đa nhánh và tạo normalized context cho WF03/WF04.",
      "modifications_made": "WF02 chưa hoàn thiện trong workflow thực tế; mới thiết kế logic. Đã thống nhất WF01 và WF02 phải tách riêng. Đã thống nhất không để AI/Guardrail/Quick Reply nằm trong WF01. Đã thống nhất Content Router trong WF02 không được loại trừ TEXT/IMAGE/AUDIO vì một batch có thể chứa nhiều loại media cùng lúc.",
      "current_code_or_config": "Cấu trúc dự kiến: Execute Workflow Trigger -> Normalize Input -> Handoff Status Check -> Human Active? -> Input Guardrail -> Guardrail Decision -> Quick Reply Matcher -> Quick Reply Decision -> Content Router (3 IF chạy song song: has_text, has_image, has_audio) -> Text/Image/Audio Processing -> Normalize Context -> WF03/WF04. Normalize Input code dự kiến: const data=$json; const combinedText=String(data.combined_text??'').trim(); const media=Array.isArray(data.media)?data.media.filter(item=>item?.type&&item?.url):[]; const hasText=combinedText.length>0; const hasImage=media.some(item=>String(item.type).toUpperCase()==='IMAGE'); const hasAudio=media.some(item=>String(item.type).toUpperCase()==='AUDIO'); return [{json:{...data,combined_text:combinedText,media,content:{has_text:hasText,has_image:hasImage,has_audio:hasAudio,has_media:media.length>0}}}]."
},
{
"node_id_or_name": "WF02 - Execute Workflow Trigger",
"purpose": "Nhận payload từ WF01.",
"modifications_made": "Chưa triển khai xong; chỉ đã thống nhất node đầu tiên của WF02.",
"current_code_or_config": "Execute Workflow Trigger, Input Source/Mode: pass through / accept all data. Dữ liệu đầu vào là payload Combine Messages của WF01."
},
{
"node_id_or_name": "WF02 - Normalize Input",
"purpose": "Chuẩn hóa payload batch từ WF01 và tạo flags has_text/has_image/has_audio để các nhánh có thể chạy song song.",
"modifications_made": "Đã thiết kế code nhưng chưa hoàn thiện thành workflow thực tế.",
"current_code_or_config": "Code: const data=$json; const combinedText=String(data.combined_text??'').trim(); const media=Array.isArray(data.media)?data.media.filter(item=>item?.type&&item?.url):[]; const hasText=combinedText.length>0; const hasImage=media.some(item=>String(item.type).toUpperCase()==='IMAGE'); const hasAudio=media.some(item=>String(item.type).toUpperCase()==='AUDIO'); return [{json:{...data,combined_text:combinedText,media,content:{has_text:hasText,has_image:hasImage,has_audio:hasAudio,has_media:media.length>0}}}];"
    },
    {
      "node_id_or_name": "WF02 - Handoff Status Check",
      "purpose": "Kiểm tra khách hiện có đang được nhân viên tiếp quản hay không trước khi AI chạy.",
      "modifications_made": "Đã thống nhất Handoff Status Check bắt buộc nằm trước AI. Nếu human active thì tuyệt đối không gọi AI/Memory/Specialist Agents để không trả lời đè nhân viên.",
      "current_code_or_config": "Query dự kiến an toàn khi chưa có record: SELECT COALESCE((SELECT is_human_active FROM human_handoff WHERE messenger_id=$1 LIMIT 1),false) AS is_human_active; Parameter $1 = messenger_id từ Normalize Input. Nếu bảng thực tế dùng cấu trúc/cột khác thì phải map theo schema hiện tại."
    },
    {
      "node_id_or_name": "WF02 - Human Active?",
      "purpose": "IF branch để chặn AI khi nhân viên đang active.",
      "modifications_made": "Đã thống nhất TRUE -> STOP AI; FALSE -> tiếp tục Guardrail/Quick Reply.",
      "current_code_or_config": "Value 1: ={{ $json.is_human_active }}. Operation: is true. TRUE: kết thúc/đưa vào Human Handoff/Staff flow; FALSE: Input Guardrail."
    },
    {
      "node_id_or_name": "WF02 - Input Guardrail",
      "purpose": "Lọc hard attack patterns và prompt injection sơ bộ trước AI; không phải lớp bảo mật duy nhất.",
      "modifications_made": "Đã cập nhật để đọc $json.combined_text thay vì $json.text. Đã thống nhất guardrail text không được chặn IMAGE/AUDIO chỉ vì chúng không có text. Đã thống nhất không nên tuyên bố Regex chặn chính xác X% nếu không có benchmark.",
      "current_code_or_config": "Code: const text=String($json.combined_text??'').trim(); const hardAttackPatterns=[/\\b(drop\\s+table|select\\s+.*\\s+from|insert\\s+into|delete\\s+from|union\\s+select)\\b/i,/;\\s*--/i,/ignore\\s+all\\s+previous\\s+instructions/i,/bo\\s+qua\\s+(tat\\s+ca\\s+)?huong\\s+dan/i,/system\\s+prompt/i,/<script\\b[^>]\*>/i,/javascript:/i]; const isSuspicious=hardAttackPatterns.some(pattern=>pattern.test(text)); return [{json:{...$json,guardrail:{checked:true,action:isSuspicious?'BLOCK':'ALLOW'}}}];"
},
{
"node_id_or_name": "WF02 - Guardrail Decision",
"purpose": "IF quyết định BLOCK/ALLOW từ Input Guardrail.",
"modifications_made": "Đã giữ logic IF dạng string equality.",
"current_code_or_config": "Value 1: ={{ $json.guardrail.action }}. Operation: is equal to. Right value: BLOCK. TRUE -> Block Response. FALSE -> Quick Reply Matcher."
},
{
"node_id_or_name": "WF02 - Quick Reply Matcher",
"purpose": "Fast Track FAQ không gọi Gemini cho câu hỏi cố định như địa chỉ shop, phí ship, đổi trả, giờ mở cửa.",
"modifications_made": "Đã chuyển logic sang đọc combined_text của batch thay vì text đơn lẻ. Đây là lớp tối ưu latency/token.",
"current_code_or_config": "Chuẩn hóa text: lower-case, đ->d, normalize NFD, loại dấu và ký tự đặc biệt. Rules hiện có: STORE_LOCATION, SHIPPING_FEE, RETURN_POLICY, OPENING_HOURS. Trả cấu trúc quick_reply:{checked:true,hit, intent, reply_text, action:'REPLY'|'MISS'}. Địa chỉ/phí ship/chính sách hiện là demo hard-coded; cần thay bằng config/KB thực tế trước production."
},
{
"node_id_or_name": "WF02 - Quick Reply Decision",
"purpose": "IF quyết định HIT/MISS.",
"modifications_made": "Đã xác định lỗi kiến trúc cũ là nhánh FALSE bị cụt; phải nối FALSE sang Handoff Status/Content Router. TRUE mới gửi Quick Reply.",
"current_code_or_config": "Value 1: ={{ $json.quick_reply.action }}. Operation: is equal to. Right value: REPLY. TRUE -> Quick Reply Response/WF08. FALSE -> tiếp tục Handoff/Content Router."
},
{
"node_id_or_name": "WF02 - Content Router",
"purpose": "Phân luồng processing theo từng loại content sau Quick Reply, nhưng cho phép nhiều nhánh chạy đồng thời vì batch có thể có TEXT + IMAGE + AUDIO.",
"modifications_made": "Đã thống nhất không dùng Switch loại trừ TEXT/IMAGE/AUDIO ở bước này. Phải dùng 3 IF song song theo content.has_text/content.has_image/content.has_audio.",
"current_code_or_config": "Text IF: ={{ $json.content.has_text }} is true. Image IF: ={{ $json.content.has_image }} is true. Audio IF: ={{ $json.content.has_audio }} is true."
}
],
"tomorrow_plan": {
"target": "wf02",
"wf02_tasks": "1) Tạo WF02 mới tên 'WF02 - Message Processing'. 2) Thêm Execute Workflow Trigger để nhận payload từ WF01. 3) Thêm Normalize Input và kiểm tra output content.has_text/has_image/has_audio. 4) Thêm Handoff Status Check trước AI; nếu human active = true thì dừng AI hoàn toàn. 5) Thêm Human Active? IF. 6) Nhánh false đi Input Guardrail. 7) Guardrail Decision: BLOCK -> Block Response; ALLOW -> Quick Reply Matcher. 8) Quick Reply Matcher đọc combined_text và xử lý FAQ cố định trước Gemini. 9) Quick Reply Decision: HIT -> Response/WF08; MISS -> Content Router. 10) Content Router dùng 3 IF song song để TEXT, IMAGE, AUDIO có thể xử lý đồng thời trong cùng một batch. 11) Text Processing tạo text_context từ combined_text. 12) Image Processing lấy media có type IMAGE để chuẩn bị Vision. 13) Audio Processing lấy media có type AUDIO để chuẩn bị STT. 14) Sau các nhánh tạo một Normalized Context thống nhất cho WF03/WF04. 15) Chưa gọi AI Supervisor cho đến khi Handoff + Guardrail + Quick Reply hoàn chỉnh. 16) Sau khi WF02 chạy ổn với TEXT, IMAGE, AUDIO, mới nối Customer Intelligence/CRM và AI Supervisor. 17) Test bắt buộc: chỉ TEXT; chỉ IMAGE; chỉ AUDIO; TEXT+IMAGE; TEXT+AUDIO; TEXT+IMAGE+AUDIO; Human Active=true; Guardrail BLOCK; Quick Reply HIT; Quick Reply MISS.",
"subsequent_workflows": "WF03 — Customer Intelligence: Customer Resolver, Customer Exists, Create Customer, Customer Profile, Preferences, Customer Events; tách Customer Profile khỏi Chat Memory. WF04 — AI Supervisor & Specialist Agents: Supervisor điều phối Sales Agent, CRM Agent, Order Agent; dùng chung Gemini model nếu phù hợp, khác nhau bằng prompt/tools/permissions. Sales Agent có Product Search, Variant Search, Inventory, RAG; CRM Agent có Customer Lookup/Update/Preferences/Events; Order Agent có Create/Lookup Order, Stock, Payment. WF05 — Visual/Multimodal processing nếu muốn tách riêng sau này; trước mắt có thể giữ Vision/STT processing trong WF02 nếu latency/độ đơn giản tốt hơn. WF06 — Payment & VietQR: tạo order, tạo QR động, nhận Payment Webhook, verify giao dịch, update order/payment. WF07 — Human Handoff: Handoff Status Check pre-AI, Handoff Detector semantic/intent/sentiment/confidence, human_handoffs, Telegram staff alert, Telegram Trigger để nhân viên reply về Messenger, reset/close handoff. WF08 — Output Validator & Response Dispatcher: validate price/stock/product/policy, retry khi fail, format TEXT/IMAGE/MULTI-MEDIA, gửi Messenger. WF09 — Monitoring & AI Logs: agent_name, intent, input/output, tool_used, latency_ms, input/output/total tokens, estimated cost, validation_status, error, handoff, rate of quick-reply hits, dashboard về sau bằng Looker Studio/Power BI. Kiến trúc tổng thể không nhất thiết phải có đúng 9 workflow vật lý nếu một số module quá nhỏ, nhưng WF01 và WF02 đã thống nhất phải tách."
},
"context_checkpoint": {
"agreed_rules": "1) Messenger là input chính và đi qua Webhook, không dùng Chat Trigger để lấy session. 2) session_id = messenger_id + '*' + page_id. 3) Customer Profile khác Chat Memory; Chat Memory chỉ lưu lịch sử hội thoại, Customer Profile lưu dữ liệu nghiệp vụ. 4) Không xây một workflow n8n khổng lồ; chia module/workflow. 5) WF01 chỉ ingestion/storage/debounce/claim/combine; không chạy AI Supervisor. 6) WF02 phụ trách Message Processing, Guardrail, Quick Reply và multimodal routing. 7) Chỉ triển khai 3 loại message hiện tại: TEXT, IMAGE, AUDIO. 8) Có thể có nhiều loại content trong cùng một batch; không dùng Switch loại trừ nhau sau Combine. 9) Quick Reply/Fast Track phải chạy trước Gemini để giảm latency/token/cost. 10) Handoff Status Check phải nằm trước AI; nếu is_human_active=true thì STOP AI, không gọi Memory/Specialist Agent để tránh AI trả lời đè nhân viên. 11) Handoff Detector là lớp phát hiện yêu cầu chuyển người; nên dựa vào semantic intent/context chứ không chỉ keyword. 12) Structured Output cho Handoff/AI được ưu tiên để hiểu ngữ nghĩa, ví dụ handoff_required=true ngay cả khi không có keyword 'nhân viên'. 13) Guardrail phải là defense-in-depth: local hard filter trước AI, parameterized SQL, tool permissions, output validation. 14) Không tuyên bố Regex chặn một phần trăm spam cụ thể nếu không benchmark. 15) Rate Limit đã được bỏ khỏi phiên bản hiện tại; có thể bổ sung sau. Debounce là cơ chế quan trọng hiện tại. 16) SQL không được nối trực tiếp user input; phải dùng query parameters/parameterized SQL. 17) AI Agent không được có quyền raw SQL tùy ý; chỉ gọi Tools với permissions rõ ràng. 18) Inventory/price/policy phải lấy từ DB/Tool/Knowledge Base, không để AI tự bịa. 19) RAG là Tool của Agent, không phải bước bắt buộc chạy sau mọi câu hỏi. 20) Vision chỉ tạo structured attributes/analysis, phải đối chiếu database trước khi khẳng định sản phẩm chính xác. 21) Audio phải được lưu URL trước và sau này chuyển thành transcript bằng STT. 22) IMAGE/AUDIO không có text không được làm Debounce fail; Debounce phải nhận JSON.stringify($json.message). 23) Combine Messages phải tạo payload thống nhất gồm session_id, messenger_id, page_id, message_ids, combined_text, media, message_count, created_at. 24) Output Dispatcher phải hỗ trợ TEXT, IMAGE và có thể gửi nhiều ảnh cho nhiều sản phẩm. 25) Output Validator nằm ngay trước Response/Dispatcher và không chỉ quét keyword; phải kiểm tra giá, tồn kho, product_id, chính sách và format. 26) Monitoring/AI logs nên chạy async, không chặn response chính. 27) Các task độc lập có thể chạy song song; những task có dependency bắt buộc tuần tự. 28) Không cần mỗi Agent một model khác nhau; có thể dùng chung Gemini model với prompt/tools/permissions khác nhau. 29) Human Handoff phải lưu DB, có status/is_human_active/reason/session/customer linkage. 30) Telegram được dùng làm kênh cho nhân viên; nhân viên có thể reply Telegram -> n8n -> Messenger và đóng handoff để AI hoạt động lại.",
    "important_notes": "1) WF01 hiện đã test thành công output Combine cho TEXT + IMAGE: combined_text='tôi là hoàng', media=[{type:'IMAGE',url:'...'}], message_count=2. 2) WF01 cũng đã test thành công AUDIO: combined_text='', media=[{type:'AUDIO',url:'https://...ogg'}], message_count=2. 3) Database thực tế là bảng conversations, cột messages dạng JSON/JSONB và đang lưu đúng IMAGE/AUDIO metadata: text, page_id, timestamp, message_id, attachments, message_type, messenger_id, attachment_urls, attachment_types. 4) Một số record cũ đã từng lưu literal '{{ $json.message_content }}' trong message.text; đó là dữ liệu test cũ và không nên lặp lại. Record mới phải lưu giá trị thật. 5) Lỗi 'null value in column session_id of relation conversations' từng xảy ra vì node trung gian làm mất session_id; đã xác định session_id phải luôn được truyền từ Data Processor/Normalize. 6) Lỗi Message Debounce 'Message is required' từng xảy ra vì dùng $json.message.text cho IMAGE/AUDIO hoặc $json.message_content ở root sau Save Message; cấu hình hiện tại phải dùng Message ={{ JSON.stringify($json.message)}}. 7) Lỗi AUDIO từng xảy ra do MESSAGE ROUTER rule AUDIO dùng '{{ $json.message_type }}' thay vì expression ={{ $json.message_type }}; đã sửa và AUDIO hiện đi qua Router. 8) Rate Limit từng trả allowed nhưng IF gặp undefined do query chỉ trả success=true; sau đó Rate Limit đã được bỏ để đơn giản hóa. 9) Hiện tại không nên sửa DB schema chỉ để xử lý media; JSON trong conversations đã đủ chứa IMAGE/AUDIO. 10) Không cần tạo VIDEO Processor/FILE Processor/OTHER Processor ở phiên bản hiện tại; scope chỉ TEXT/IMAGE/AUDIO. 11) Trong WF02, Handoff Status Check phải dùng query an toàn khi customer chưa có handoff record, ví dụ COALESCE((SELECT is_human_active ...), false). 12) Quick Reply hiện có demo data hard-coded như địa chỉ 123 Đường ABC, phí ship 30k, đổi trả 7 ngày, giờ 8h-22h; phải thay bằng data thực khi production. 13) Một Page Access Token/Facebook token đã từng xuất hiện trực tiếp trong node JSON và được gửi trong chat; không lặp lại token trong context tương lai, nên rotate/revoke token đó và chuyển sang Credential. 14) Telegram bot credential/Chat ID đã test thành công trong phiên trước; Telegram webhook từng lỗi vì n8n Production URL là http://localhost:5678, sau đó đã cấu hình ngrok/public HTTPS và bot test gửi tin được. 15) Hệ thống đang dùng PostgreSQL + Redis + n8n + Gemini + Messenger + Telegram. 16) Nếu tiếp tục tối ưu hiệu năng, ưu tiên Debounce, Quick Reply, parallel branches và async logging hơn là thêm quá nhiều workflow boundaries. 17) Khi làm WF02, không gọi AI cho Quick Reply HIT. 18) Khi khách gửi TEXT+IMAGE+AUDIO trong một batch, cả 3 nhánh xử lý phải có thể chạy, sau đó merge/normalize về một context trước Supervisor. 19) Source of Truth trong MASTER_PROJECT.md quy định thứ tự module và các important rules; không tự ý đổi architecture nếu chưa có lý do rõ ràng. 20) Trạng thái phiên hiện tại: WF01 gần hoàn thiện và đã có output thực tế hợp lệ; WF02 mới ở mức thiết kế/chuẩn bị node, chưa hoàn thiện; WF03 trở đi chưa triển khai hoàn chỉnh."
}
}
